# Dental Clinic Backend

A RESTful API built with **Node.js**, **Express**, and **MongoDB** (Mongoose) to manage dental clinic operations.

## 🚀 Features
* **Patient Management**: Full CRUD operations for patient records.
* **Appointment System**: Create and track appointments linked to specific patients.
* **Data Population**: Uses Mongoose population to join Patient data with Appointment records.
* **Validations**: Request data validation using `express-validator`.

## 🛠️ Tech Stack
* **Runtime**: Node.js
* **Framework**: Express.js
* **Database**: MongoDB (via Mongoose)
* **Tools**: Postman (for testing), Nodemon (for development)

## 🏗️ Getting Started
1. Clone the repo: `git clone https://github.com/danielculeabin/dental-app-backend.git`
2. Install dependencies: `npm install`
3. Ensure MongoDB is running locally on port `27017`.
4. Start the server: `npm run dev`
